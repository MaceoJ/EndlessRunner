
cam = view_camera[0];
view_w_half = camera_get_view_width(cam)/4; //centers width of cam between player and left of screen
view_h_half = camera_get_view_height(cam)/2; //centers height of cam to player
xTo = x;  //where we want x to be
yTo = y;  //where we want y to be